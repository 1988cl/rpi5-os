----------QEMU-------------

# 导出 QEMU virt 平台的设备树二进制文件
qemu-system-aarch64 -machine virt -machine dumpdtb=virt.dtb

# 反编译二进制设备树为文本格式
dtc -I dtb -O dts -o virt.dts virt.dtb

#startup the qemu
qemu-system-aarch64 -machine virt -cpu cortex-a72 -m 128M -kernel kernel8.img
